services:
  api:
    build:
      target: production
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    restart: always
    volumes: []   # no bind mount in production

  db:
    restart: always

  redis:
    restart: always

  langfuse:
    restart: always
